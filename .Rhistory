textOutput("scatter_text")
), # end of Conditional Panel
)
), # end of Explore nav_panel
# ---- Tab 2: written analysis / summary --------------------------------
nav_panel(
title = "Analysis",
analysis_content   # see R/ui-text.R
),
# ---- Tab 3: about the survey ------------------------------------------
nav_panel(
title = "About",
about_content      # see R/ui-text.R
),
)
server <- function(input, output, session) {
source(file.path("server-plots.R"), local = TRUE)$value
}
shinyApp(ui = ui, server = server)
library(shiny)
library(bslib)
library(shinycssloaders) # for loading screens
library(colourpicker)    # for the colour pickers
# Load the cleaned survey data once, when the app starts. Use a RELATIVE path so
# it also works after deployment to Posit Connect Cloud.
survey <- read.csv("survey.csv")
# Shared helpers and the sidebar/tab building blocks; see the R/ folder.
source(file.path("R", "ui-text.R"))
source(file.path("R", "site-helper.R"))
source(file.path("R", "ui-extra.R"))
source(file.path("R", "ui-controls.R"))
source(file.path("R", "ui-latex.R"))
# Globally setting the spinner colour and type (will change)
options(spinner.type = 8, spinner.color = "#6990EE")
ui <- page_navbar(
theme = bs_theme(version = 5,
bootswatch = "flatly",
"navbar-bg" = "#2C3E50"), # may want to change theme
title = "STA304 Student Survey Explorer",
# ---- Tab 1: the interactive explorer (has its own sidebar) -------------
nav_panel(
title = "Explore",
layout_sidebar(
sidebar = sidebar(
selectInput(inputId = "plot_type",
label = "What would you like to see?",
choices = list("Distribution of one variable"      = "dist",
"Comparison across a group"          = "compare",
"Relationship between two variables" = "scatter"),
selected = "dist"),
selectInput(inputId = "modify_type",
label = "What would you like to modify?",
choices = list("The data (filters)"     = "data",
"The graph (appearance)" = "graph"),
selected = "data"),
var_selectors,    # which variable(s) to plot; see R/ui-controls.R
data_filters,     # demographic filters;        see R/ui-controls.R
graph_appearance, # colours/symbols/limits;     see R/ui-controls.R
width = 400,
open = "always"
),
# main content: one conditionalPanel per plot type, each with a plot and a
# short written interpretation underneath it.
conditionalPanel(
condition = "input.plot_type == 'dist'",
withSpinner(plotOutput("dist_plot")),
br(),
textOutput("dist_text")
), # end of Conditional Panel
conditionalPanel(
condition = "input.plot_type == 'compare'",
withSpinner(plotOutput("compare_plot")),
br(),
textOutput("compare_text")
), # end of Conditional Panel
conditionalPanel(
condition = "input.plot_type == 'scatter'",
withSpinner(plotOutput("scatter_plot")),
br(),
textOutput("scatter_text")
), # end of Conditional Panel
)
), # end of Explore nav_panel
# ---- Tab 2: written analysis / summary --------------------------------
nav_panel(
title = "Analysis",
analysis_content   # see R/ui-text.R
),
# ---- Tab 3: about the survey ------------------------------------------
nav_panel(
title = "About",
about_content      # see R/ui-text.R
),
)
server <- function(input, output, session) {
source(file.path("server-plots.R"), local = TRUE)$value
}
shinyApp(ui = ui, server = server)
library(shiny)
library(bslib)
library(shinycssloaders) # for loading screens
library(colourpicker)    # for the colour pickers
# Load the cleaned survey data once, when the app starts. Use a RELATIVE path so
# it also works after deployment to Posit Connect Cloud.
survey <- read.csv("survey.csv")
# Shared helpers and the sidebar/tab building blocks; see the R/ folder.
source(file.path("R", "ui-text.R"))
source(file.path("R", "site-helper.R"))
source(file.path("R", "ui-extra.R"))
source(file.path("R", "ui-controls.R"))
source(file.path("R", "ui-latex.R"))
# Globally setting the spinner colour and type (will change)
options(spinner.type = 8, spinner.color = "#6990EE")
ui <- page_navbar(
theme = bs_theme(version = 5,
bootswatch = "flatly",
"navbar-bg" = "#2C3E50"), # may want to change theme
title = "STA304 Student Survey Explorer",
# ---- Tab 1: the interactive explorer (has its own sidebar) -------------
nav_panel(
title = "Explore",
layout_sidebar(
sidebar = sidebar(
selectInput(inputId = "plot_type",
label = "What would you like to see?",
choices = list("Distribution of one variable"      = "dist",
"Comparison across a group"          = "compare",
"Relationship between two variables" = "scatter"),
selected = "dist"),
selectInput(inputId = "modify_type",
label = "What would you like to modify?",
choices = list("The data (filters)"     = "data",
"The graph (appearance)" = "graph"),
selected = "data"),
var_selectors,    # which variable(s) to plot; see R/ui-controls.R
data_filters,     # demographic filters;        see R/ui-controls.R
graph_appearance, # colours/symbols/limits;     see R/ui-controls.R
width = 400,
open = "always"
),
# main content: one conditionalPanel per plot type, each with a plot and a
# short written interpretation underneath it.
conditionalPanel(
condition = "input.plot_type == 'dist'",
withSpinner(plotOutput("dist_plot")),
br(),
textOutput("dist_text")
), # end of Conditional Panel
conditionalPanel(
condition = "input.plot_type == 'compare'",
withSpinner(plotOutput("compare_plot")),
br(),
textOutput("compare_text")
), # end of Conditional Panel
conditionalPanel(
condition = "input.plot_type == 'scatter'",
withSpinner(plotOutput("scatter_plot")),
br(),
textOutput("scatter_text")
), # end of Conditional Panel
)
), # end of Explore nav_panel
# ---- Tab 2: written analysis / summary --------------------------------
nav_panel(
title = "Analysis",
analysis_content   # see R/ui-text.R
),
# ---- Tab 3: about the survey ------------------------------------------
nav_panel(
title = "About",
about_content      # see R/ui-text.R
),
)
server <- function(input, output, session) {
source(file.path("server-plots.R"), local = TRUE)$value
}
shinyApp(ui = ui, server = server)
library(shiny)
library(bslib)
library(shinycssloaders) # for loading screens
library(colourpicker)    # for the colour pickers
# Load the cleaned survey data once, when the app starts. Use a RELATIVE path so
# it also works after deployment to Posit Connect Cloud.
survey <- read.csv("survey.csv")
# Shared helpers and the sidebar/tab building blocks; see the R/ folder.
source(file.path("R", "ui-text.R"))
source(file.path("R", "site-helper.R"))
source(file.path("R", "ui-extra.R"))
source(file.path("R", "ui-controls.R"))
source(file.path("R", "ui-latex.R"))
# Globally setting the spinner colour and type (will change)
options(spinner.type = 8, spinner.color = "#6990EE")
ui <- page_navbar(
theme = bs_theme(version = 5,
bootswatch = "flatly",
"navbar-bg" = "#2C3E50"), # may want to change theme
title = "STA304 Student Survey Explorer",
# load KaTeX so $...$ math renders anywhere in the app; see R/ui-latex.R.
# (page_navbar's `header` is the right slot for this — a loose element in the
# main `...` would be treated as a nav item.)
header = latex_tags,
# ---- Tab 1: the interactive explorer (has its own sidebar) -------------
nav_panel(
title = "Explore",
layout_sidebar(
sidebar = sidebar(
selectInput(inputId = "plot_type",
label = "What would you like to see?",
choices = list("Distribution of one variable"      = "dist",
"Comparison across a group"          = "compare",
"Relationship between two variables" = "scatter"),
selected = "dist"),
selectInput(inputId = "modify_type",
label = "What would you like to modify?",
choices = list("The data (filters)"     = "data",
"The graph (appearance)" = "graph"),
selected = "data"),
var_selectors,    # which variable(s) to plot; see R/ui-controls.R
data_filters,     # demographic filters;        see R/ui-controls.R
graph_appearance, # colours/symbols/limits;     see R/ui-controls.R
width = 400,
open = "always"
),
# main content: one conditionalPanel per plot type, each with a plot and a
# short written interpretation underneath it.
conditionalPanel(
condition = "input.plot_type == 'dist'",
withSpinner(plotOutput("dist_plot")),
br(),
textOutput("dist_text")
), # end of Conditional Panel
conditionalPanel(
condition = "input.plot_type == 'compare'",
withSpinner(plotOutput("compare_plot")),
br(),
textOutput("compare_text")
), # end of Conditional Panel
conditionalPanel(
condition = "input.plot_type == 'scatter'",
withSpinner(plotOutput("scatter_plot")),
br(),
textOutput("scatter_text")
), # end of Conditional Panel
)
), # end of Explore nav_panel
# ---- Tab 2: written analysis / summary --------------------------------
nav_panel(
title = "Analysis",
analysis_content   # see R/ui-text.R
),
# ---- Tab 3: about the survey ------------------------------------------
nav_panel(
title = "About",
about_content      # see R/ui-text.R
),
)
server <- function(input, output, session) {
source(file.path("server-plots.R"), local = TRUE)$value
}
shinyApp(ui = ui, server = server)
runApp()
library(shiny)
library(bslib)
library(shinycssloaders) # for loading screens
library(colourpicker)    # for the colour pickers
# Load the cleaned survey data once, when the app starts. Use a RELATIVE path so
# it also works after deployment to Posit Connect Cloud.
survey <- read.csv("survey.csv")
# Shared helpers and the sidebar/tab building blocks; see the R/ folder.
source(file.path("R", "ui-text.R"))
source(file.path("R", "site-helper.R"))
source(file.path("R", "ui-extra.R"))
source(file.path("R", "ui-controls.R"))
source(file.path("R", "ui-latex.R"))
# Globally setting the spinner colour and type (will change)
options(spinner.type = 8, spinner.color = "#6990EE")
ui <- page_navbar(
theme = bs_theme(version = 5,
bootswatch = "flatly",
"navbar-bg" = "#2C3E50"), # may want to change theme
title = "STA304 Student Survey Explorer",
# load KaTeX so $...$ math renders anywhere in the app; see R/ui-latex.R.
# (page_navbar's `header` is the right slot for this — a loose element in the
# main `...` would be treated as a nav item.)
header = latex_tags,
# ---- Tab 1: the interactive explorer (has its own sidebar) -------------
nav_panel(
title = "Explore",
layout_sidebar(
sidebar = sidebar(
selectInput(inputId = "plot_type",
label = "What would you like to see?",
choices = list("Distribution of one variable"      = "dist",
"Comparison across a group"          = "compare",
"Relationship between two variables" = "scatter"),
selected = "dist"),
selectInput(inputId = "modify_type",
label = "What would you like to modify?",
choices = list("The data (filters)"     = "data",
"The graph (appearance)" = "graph"),
selected = "data"),
var_selectors,    # which variable(s) to plot; see R/ui-controls.R
data_filters,     # demographic filters;        see R/ui-controls.R
graph_appearance, # colours/symbols/limits;     see R/ui-controls.R
width = 400,
open = "always"
),
# main content: one conditionalPanel per plot type, each with a plot and a
# short written interpretation underneath it.
conditionalPanel(
condition = "input.plot_type == 'dist'",
withSpinner(plotOutput("dist_plot")),
br(),
textOutput("dist_text")
), # end of Conditional Panel
conditionalPanel(
condition = "input.plot_type == 'compare'",
withSpinner(plotOutput("compare_plot")),
br(),
textOutput("compare_text")
), # end of Conditional Panel
conditionalPanel(
condition = "input.plot_type == 'scatter'",
withSpinner(plotOutput("scatter_plot")),
br(),
textOutput("scatter_text")
), # end of Conditional Panel
)
), # end of Explore nav_panel
# ---- Tab 2: written analysis / summary --------------------------------
nav_panel(
title = "Analysis",
analysis_content   # see R/ui-text.R
),
# ---- Tab 3: about the survey ------------------------------------------
nav_panel(
title = "About",
about_content      # see R/ui-text.R
),
)
server <- function(input, output, session) {
source(file.path("server-plots.R"), local = TRUE)$value
}
shinyApp(ui = ui, server = server)
library(shiny)
library(bslib)
library(shinycssloaders) # for loading screens
library(colourpicker)    # for the colour pickers
# Load the cleaned survey data once, when the app starts. Use a RELATIVE path so
# it also works after deployment to Posit Connect Cloud.
survey <- read.csv("survey.csv")
# Shared helpers and the sidebar/tab building blocks; see the R/ folder.
source(file.path("R", "ui-text.R"))
source(file.path("R", "site-helper.R"))
source(file.path("R", "ui-extra.R"))
source(file.path("R", "ui-controls.R"))
source(file.path("R", "ui-latex.R"))
# Globally setting the spinner colour and type (will change)
options(spinner.type = 8, spinner.color = "#6990EE")
ui <- page_navbar(
theme = bs_theme(version = 5,
bootswatch = "flatly",
"navbar-bg" = "#2C3E50"), # may want to change theme
title = "STA304 Student Survey Explorer",
# load KaTeX so $...$ math renders anywhere in the app; see R/ui-latex.R.
# (page_navbar's `header` is the right slot for this — a loose element in the
# main `...` would be treated as a nav item.)
header = latex_tags,
# ---- Tab 1: the interactive explorer (has its own sidebar) -------------
nav_panel(
title = "Explore",
layout_sidebar(
sidebar = sidebar(
selectInput(inputId = "plot_type",
label = "What would you like to see?",
choices = list("Distribution of one variable"      = "dist",
"Comparison across a group"          = "compare",
"Relationship between two variables" = "scatter"),
selected = "dist"),
selectInput(inputId = "modify_type",
label = "What would you like to modify?",
choices = list("The data (filters)"     = "data",
"The graph (appearance)" = "graph"),
selected = "data"),
var_selectors,    # which variable(s) to plot; see R/ui-controls.R
data_filters,     # demographic filters;        see R/ui-controls.R
graph_appearance, # colours/symbols/limits;     see R/ui-controls.R
width = 400,
open = "always"
),
# main content: one conditionalPanel per plot type, each with a plot and a
# short written interpretation underneath it.
conditionalPanel(
condition = "input.plot_type == 'dist'",
withSpinner(plotOutput("dist_plot")),
br(),
textOutput("dist_text")
), # end of Conditional Panel
conditionalPanel(
condition = "input.plot_type == 'compare'",
withSpinner(plotOutput("compare_plot")),
br(),
textOutput("compare_text")
), # end of Conditional Panel
conditionalPanel(
condition = "input.plot_type == 'scatter'",
withSpinner(plotOutput("scatter_plot")),
br(),
textOutput("scatter_text")
), # end of Conditional Panel
)
), # end of Explore nav_panel
# ---- Tab 2: written analysis / summary --------------------------------
nav_panel(
title = "Analysis",
analysis_content   # see R/ui-text.R
),
# ---- Tab 3: about the survey ------------------------------------------
nav_panel(
title = "About",
about_content      # see R/ui-text.R
),
)
server <- function(input, output, session) {
source(file.path("server-plots.R"), local = TRUE)$value
}
shinyApp(ui = ui, server = server)
library(shiny)
library(bslib)
library(shinycssloaders) # for loading screens
library(colourpicker)    # for the colour pickers
# Load the cleaned survey data once, when the app starts. Use a RELATIVE path so
# it also works after deployment to Posit Connect Cloud.
survey <- read.csv("survey.csv")
# Shared helpers and the sidebar/tab building blocks; see the R/ folder.
source(file.path("R", "ui-text.R"))
source(file.path("R", "site-helper.R"))
source(file.path("R", "ui-extra.R"))
source(file.path("R", "ui-controls.R"))
source(file.path("R", "ui-latex.R"))
# Globally setting the spinner colour and type (will change)
options(spinner.type = 8, spinner.color = "#6990EE")
ui <- page_navbar(
theme = bs_theme(version = 5,
bootswatch = "flatly",
"navbar-bg" = "#2C3E50"), # may want to change theme
title = "STA304 Student Survey Explorer",
# load KaTeX so $...$ math renders anywhere in the app; see R/ui-latex.R.
# (page_navbar's `header` is the right slot for this — a loose element in the
# main `...` would be treated as a nav item.)
header = latex_tags,
# ---- Tab 1: the interactive explorer (has its own sidebar) -------------
nav_panel(
title = "Explore",
layout_sidebar(
sidebar = sidebar(
selectInput(inputId = "plot_type",
label = "What would you like to see?",
choices = list("Distribution of one variable"      = "dist",
"Comparison across a group"          = "compare",
"Relationship between two variables" = "scatter"),
selected = "dist"),
selectInput(inputId = "modify_type",
label = "What would you like to modify?",
choices = list("The data (filters)"     = "data",
"The graph (appearance)" = "graph"),
selected = "data"),
var_selectors,    # which variable(s) to plot; see R/ui-controls.R
data_filters,     # demographic filters;        see R/ui-controls.R
graph_appearance, # colours/symbols/limits;     see R/ui-controls.R
width = 400,
open = "always"
),
# main content: one conditionalPanel per plot type, each with a plot and a
# short written interpretation underneath it.
conditionalPanel(
condition = "input.plot_type == 'dist'",
withSpinner(plotOutput("dist_plot")),
br(),
textOutput("dist_text")
), # end of Conditional Panel
conditionalPanel(
condition = "input.plot_type == 'compare'",
withSpinner(plotOutput("compare_plot")),
br(),
textOutput("compare_text")
), # end of Conditional Panel
conditionalPanel(
condition = "input.plot_type == 'scatter'",
withSpinner(plotOutput("scatter_plot")),
br(),
textOutput("scatter_text")
), # end of Conditional Panel
)
), # end of Explore nav_panel
# ---- Tab 2: written analysis / summary --------------------------------
nav_panel(
title = "Analysis",
analysis_content   # see R/ui-text.R
),
# ---- Tab 3: about the survey ------------------------------------------
nav_panel(
title = "About",
about_content      # see R/ui-text.R
),
)
server <- function(input, output, session) {
source(file.path("server-plots.R"), local = TRUE)$value
}
shinyApp(ui = ui, server = server)
library(shiny)
detach("package:shiny", unload = TRUE)
